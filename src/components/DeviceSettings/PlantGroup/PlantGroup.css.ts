import { style, globalStyle } from "@vanilla-extract/css";
import { palette, breakpoints } from "../../../theme";

export const styles = {
  plantTitle: style({
    marginTop: "1rem",
    marginRight: "auto",
    marginLeft: "auto",
    alignSelf: "center",
  }),
};
